# MangaCanvas 待办清单

> 当前策略：**纯前端 Mock 闭环**，暂不接入真实 API。以下按优先级与模块组织。
> 
> **后端开发请参考**: [`BACKEND_API_SPEC.md`](./BACKEND_API_SPEC.md)

---

## ✅ 已完成

### 架构优化 ✅
- [x] **项目资产状态重构** (2025-04-05)
  - 新增 `src/stores/projectStore.ts` - 集中管理项目资产状态
  - 新增 `src/types/index.ts` - 统一类型定义
  - 新增 `src/data/initialData.ts` - 分离 Mock 数据
  - `ProjectDetail` 代码行数 -70%，从 374 行减至 112 行
  - 各 Tab 组件直接订阅 Store，移除 props 传递

- [x] 画廊页面（`/gallery`）与导航
- [x] Dashboard 新建项目弹框（含表单验证与列表实时更新）
- [x] 片段弹框 `EpisodeCreator.tsx` 提交逻辑（支持新建片段并实时显示）

### P0 核心流程闭环 ✅
- [x] **场景管理**
  - `ScenesTab` state 化（支持外部控制或内部状态）
  - `SceneCreator` 添加 `onCreate` 回调，支持表单提交
  - 参考图本地上传预览（`URL.createObjectURL`）
  - 卡片操作菜单：删除、复制、查看详情
  
- [x] **角色管理**
  - `CharactersTab` state 化
  - `CharacterCreator` 添加 `onCreate` 回调
  - 角色定位选择（主角/配角）
  - 参考图本地上传预览
  - 提示词模板一键填入
  - 种子复制功能
  - 卡片操作菜单：编辑、删除、复制
  
- [x] **物品管理**
  - 创建 `ObjectsTab.tsx` 完整组件
  - 创建 `ObjectCreator.tsx` 抽屉组件（简化版：名称+生成方式+上传）
  - ~~物品类型标签（武器/道具/服装/场景装饰）~~（已简化）
  - ~~关联场景选择~~（已简化）
  - 参考图本地上传（单张+批量）
  - ~~3D 预览占位区~~（已简化）
  - 卡片操作菜单：删除、复制

---

## 🔴 P0 — 核心创作流程闭环（剩余）

### 2. 卡片操作菜单完善
**目标**：所有卡片上的悬停按钮都有实际反馈。

#### 2.1 场景卡片
- [ ] `编辑详情` / `继续工作` 按钮 → 打开 `SceneCreator` 抽屉并回显当前数据（编辑态）

#### 2.2 角色卡片
- [ ] `编辑` 按钮 → 打开 `CharacterCreator` 抽屉并回显数据（编辑态）

#### 2.3 片段卡片 ✅
- [x] `查看详情` / `继续编辑` 按钮 → 导航到 `EpisodeDetail` 详情页
- [x] **片段详情页** (`/project/:id/episode/:episodeId`)
  - 片段概览：名称、状态、进度、描述
  - 登场角色列表（可添加/查看）
  - 使用场景展示（缩略图+镜头数）
  - 道具物品网格
  - 分镜时间线（可点击编辑）
  - 制作进度统计
  - **大大的「开始/继续创作」按钮**
- [ ] `⋯` 更多按钮 → DropdownMenu：删除、复制

#### 2.4 物品卡片
- [ ] 编辑功能：打开 `ObjectCreator` 抽屉回显数据（编辑态）

---

## 🟠 P1 — Dashboard 与导航闭环

### 3. Dashboard 功能补全

#### 3.1 新建资源（侧边栏）
- [ ] 创建 `ResourceCreator.tsx` 轻量弹框
  - 字段：资源名称、资源类型（图片/视频/3D模型）、文件上传
  - 或使用最小闭环：`alert("新建资源：选择文件上传")`

#### 3.2 项目卡片更多操作
- [ ] `⋯` DropdownMenu 提供：重命名、删除、复制项目
- [ ] 操作后更新 `projects` state

#### 3.3 查看全部项目
- [ ] 添加视图切换：网格 / 列表
- [ ] 或添加过滤：全部 / 进行中 / 已完成

#### 3.4 侧边栏导航
- [ ] 项目 → 当前页高亮
- [ ] 资源 / 团队 / 设置 / 分析 → 创建对应的空壳页面并导航
- [ ] 或暂时用 `alert("功能开发中")`

### 4. 公共页面补全（Footer & 首页）

#### 4.1 静态页面创建 ✅
- [x] **服务条款页面** (`/terms`)
  - 完整的服务条款文本（8个章节）
  - 返回导航按钮
  - 响应式布局

- [x] **隐私政策页面** (`/privacy`)
  - 完整的隐私政策（11个章节）
  - 与整体风格一致的布局

- [x] **联系我们页面** (`/contact`)
  - 联系表单：姓名、邮箱、主题、消息
  - 提交后显示成功状态
  - 联系信息卡片展示

- [x] **工作流页面** (`/workflow`)
  - 6步创作流程展示
  - 特性介绍网格
  - CTA 引导

#### 4.2 Footer 链接修复 ✅
- [x] 更新 `App.tsx` 路由，添加新页面路由
- [x] 修复首页/Login/Dashboard/Gallery 的 Footer 链接指向真实页面

---

## 🟡 P2 — 认证与首页交互

### 5. 认证系统 Mock

#### 5.1 登录 / 注册表单提交 ✅
- [x] 表单提交时写入 `localStorage.setItem("manga-user", JSON.stringify({email, name}))`
- [x] 登录成功后 `navigate("/dashboard")`
- [x] 登录页自动填充 Mock 数据（邮箱：`demo@mangacanvas.com`，密码：`123456`）
- [ ] Dashboard / App Header 根据 `localStorage` 显示用户头像和「退出登录」

#### 5.2 忘记密码 ✅
- [x] 点击后 `alert("重置链接已发送至邮箱（Mock）")`

#### 5.3 第三方登录 ✅
- [x] Google / GitHub 按钮点击后 `alert("第三方登录演示：已模拟登录成功")`
- [x] 写入相同的 `localStorage` 并跳转 `/dashboard`

### 6. 首页（Landing）交互

#### 6.1 观看演示
- ~~[ ] 点击后弹出 Dialog，内嵌演示 GIF 或说明文字~~ **（不需要）**

#### 6.2 场景编排卡片
- [ ] 点击后跳转 `/dashboard` 或 `/project/1`

#### 6.3 CTA 按钮
- [x] 「免费开始」→ 跳转 `/dashboard`（已实现）
- ~~[ ] 「了解更多」→ 滚动到功能介绍区域~~ **（不需要）**

---

## 🟢 P3 — 项目详情页的辅助功能

### 7. 抽屉/弹框内的微交互

#### 7.1 一键填入提示词框架 ✅
- [x] `CharacterCreator` 描述框旁添加「使用模板」按钮
- [x] 点击后自动填充标准角色描写模板

#### 7.2 参考图上传 ✅
- [x] 场景 & 角色抽屉支持参考图上传
- [x] 使用隐藏的 `<input type="file">` + `URL.createObjectURL(file)` 实现本地预览

#### 7.3 种子复制按钮 ✅
- [x] `CharacterCreator` 种子值旁添加复制按钮
- [x] 调用 `navigator.clipboard.writeText(seedValue)`
- [x] 成功后显示「已复制」提示

### 8. 顶部操作按钮模拟

#### 8.1 导出视频
- [ ] 点击后 `alert("开始导出视频（Mock 任务已加入队列）")`

#### 8.2 分享
- [ ] 点击后 `alert("分享链接已复制到剪贴板")`
- [ ] 或实际复制当前 URL

#### 8.3 批量删除
- [ ] 点击进入选择模式
- [ ] 卡片显示复选框
- [ ] 勾选后确认删除并更新 state

#### 8.4 通知中心
- [ ] 打开下拉菜单或 Sheet
- [ ] 展示 Mock 通知列表（生成完成、系统消息等）

#### 8.5 新建资源（项目详情侧边栏）
- [ ] 复用 Dashboard 的新建资源逻辑

---

## 🔵 P4 — 高级功能与占位页面

### 9. 融合生图 (`fusion` tab)
**状态**：~~不需要~~（暂不实现）

~~- [ ] **创建 `FusionTab.tsx`**~~
~~- [ ] **创建 `FusionCreator.tsx`（可选）**~~
~~- [ ] **Mock 生成流程**~~

### 10. 图片改创 (`remix` tab)
**目标**：上传原图 + 风格选择 + 生成结果。

- [ ] **创建 `RemixTab.tsx`**
  - 左侧：原图上传区
  - 中部：风格选择网格（赛博朋克/水墨/油画/像素等）
  - 右侧：生成结果对比（原图 vs 改创图）

- [ ] **创建 `RemixCreator.tsx`（可选）**
  - 高级设置抽屉

- [ ] **Mock 生成流程**
  - 选择风格后显示生成中状态
  - 2秒后显示风格化后的 Mock 图

### 11. Pricing CTA 闭环
- [ ] **免费开始** → 跳转 `/dashboard`
- [ ] **立即升级** → 弹出支付模拟 Dialog
- [ ] **联系销售** → 跳转 `/contact`

---

## 🟣 P5 — 体验优化与 Polish

### 12. 动画与过渡
- [ ] 卡片添加进入动画（stagger 效果）
- [ ] 抽屉/弹框添加平滑开关动画
- [ ] 页面切换添加过渡效果

### 13. 空状态设计
- [ ] 各标签页添加空状态插图和文案
- [ ] 引导用户创建第一个项目/场景/角色

### 14. 响应式优化
- [ ] 移动端侧边栏折叠为汉堡菜单
- [ ] 小屏幕下卡片网格自适应调整
- [ ] 抽屉在移动端全屏显示

### 15. 键盘快捷键
- [ ] `Ctrl/Cmd + N`：新建（根据当前标签页）
- [ ] `Ctrl/Cmd + S`：保存（Mock 提示）
- [ ] `Esc`：关闭抽屉/弹框

---

## ⚪ P6 — 后端接入（长期搁置）

- [ ] 替换 Mock 数据为真实 API
- [ ] 接入 React Query 或等效方案管理服务端状态
- [ ] 用户认证状态全局管理（Context 或 Zustand）
- [ ] 接入真实的 AI 生成服务
- [ ] 文件上传接口（OSS / S3）

---

## 📊 任务统计

| 优先级 | 任务数 | 完成数 | 进度 |
|--------|--------|--------|------|
| ✅ P0 - 架构与核心 | 5 | 5 | 100% |
| 🔴 P0 - 核心流程 | 24 | 19 | 79.2% |
| 🟠 P1 - Dashboard | 12 | 5 | 41.7% |
| 🟡 P2 - 认证与首页 | 6 | 3 | 50% |
| 🟢 P3 - 辅助功能 | 11 | 3 | 27.3% |
| 🔵 P4 - 高级功能 | 7 | 0 | 0% |
| 🟣 P5 - 体验优化 | 4 | 0 | 0% |
| ⚪ P6 - 后端接入 | 5 | 0 | 0% |
| **总计** | **74** | **35** | **47.3%** |

---

*最后更新：2026-04-05*

---

## 📝 更新日志

### 2026-04-05
- ✅ 简化 `ObjectCreator`：精简为名称+生成方式+上传（对齐参考设计）
- ✅ 调整功能范围：移除"观看演示"、"融合生图"等不需要的功能
- ✅ 简化 Dashboard：移除"创建新项目"卡片（保留顶部新建按钮）
- ✅ 完成 Footer 静态页面（服务条款、隐私政策、联系我们、工作流）
- ✅ 完成项目资产状态重构（Store + Types + Data 分层）
